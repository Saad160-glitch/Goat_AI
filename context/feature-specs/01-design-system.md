Read 'AGENTS.MD' before starting.

We're adding the design systemand UI primitive components.

Install and configure 'shadcn/ui'.

Add these shadcn components:
-Buttons
-Card
-Dialog
-Input
-Tabs
-Textarea
-ScrollArea

Do not modify the generated 'components/ui/*' files after installation.

Also install 'lucid-react'.

Create 'lib/utils.ts' with a resuable 'cn()' helper for mirging Tailwing classes.

Ensure all the components match the existing dark theme in 'globals.css'.

### Check when done
-All components import without errors.
-'cn()' works properly.
-No default light styling appears.
